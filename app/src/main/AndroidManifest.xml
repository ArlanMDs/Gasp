<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.ufersa.arlan.gasp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SelecionarTipoActivity" />
        <activity android:name=".activities.CadastrarUsuarioActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.ClienteMainActivity" />
        <activity android:name=".activities.ServicosAbertosActivity" />
        <activity android:name=".activities.ServicoStatusActivity" />
        <activity android:name=".activities.ServicosFinalizadosActivity" />
        <activity android:name=".activities.PrestadorMainActivity" />
        <activity android:name=".activities.CadastrarServicoActivity" />
        <activity android:name=".activities.CadastrarFuncionarioActivity" />
        <activity android:name=".activities.ListarServicosActivity" />
        <activity android:name=".activities.ModificarServicoActivity"></activity>
    </application>

</manifest>