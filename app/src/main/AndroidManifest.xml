<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.ufersa.arlan.gasp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.CadastrarUsuarioActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".cliente_activities.ClienteMainActivity" />
        <activity android:name=".cliente_activities.ServicosAbertosActivity" />
        <activity android:name=".cliente_activities.ServicoStatusActivity" />
        <activity android:name=".cliente_activities.ServicosFinalizadosActivity" />
        <activity android:name=".prestador_activities.PrestadorMainActivity" />
        <activity android:name=".prestador_activities.CadastrarServicoActivity" />
        <activity android:name=".prestador_activities.CadastrarFuncionarioActivity" />
        <activity android:name=".prestador_activities.ListarServicosActivity" />
        <activity android:name=".prestador_activities.ModificarServicoActivity"/>
    </application>

</manifest>